# DSHA 状态栏灵动胶囊与焦点通知改造备忘 (通知修改README)

本文档整理了 DSHA 在 Android 16 (AOSP Live Updates) 与小米澎湃 OS (HyperOS / HyperIsland 灵动岛) 通知体系中的全部技术排查、架构重构与视觉体验演进记录。

---

## 一、 核心演进与排查全记录

### 1. 为什么放弃 `RemoteViews` 自定义 XML 布局？
* **痛点问题**：之前使用自定义布局 (`notification_live_dark.xml`) 时，在浅色模式下通知栏四周会出现一圈**突兀的白边框**，且卡片在通知栏收起态下被系统强制限制为 64dp 高度，导致底部文字**被纵向压扁裁切**。
* **原因分析**：Android 12+ 系统在渲染第三方 App 的 `RemoteViews` 时，会在外层强制套用系统默认的浅色通知卡片外壳；且 XML 中若使用 `<View>` 等非白名单标签，会直接导致 SystemUI 跨进程渲染崩溃并静默丢弃通知。
* **解决办法**：彻底移除 `RemoteViews` 自绘布局，回归 Android 标准 `NotificationCompat.Builder` 原生模板，由系统自身负责卡片绘制，彻底根治白边与压扁。

### 2. 小米澎湃 OS 灵动岛（Focus Notification）协议完美适配
* **左耳图标与副标题**：
  * 将大肥鱼小蓝鲸图标通过 Python 漫水算法抠除原始伪棋盘格背景，生成 **100% 纯透明底 PNG (`ic_whale_logo.png`)**；
  * 在 `param_island.bigIslandArea.imageTextInfoLeft` 中完整配置 `picInfo`（指向 `miui.focus.pic_big_island`）与 `textInfo`（标题固定为 `大肥鱼`），彻底解决左耳一片漆黑空白的问题。
* **右耳状态自适应弹性拉伸**：
  * 引入 `compactCapsuleText` 算法，将当前工具调用和状态严格压缩至 **4~6 个字**（如 `执行命令中`、`读取文件中`、`分析执行中`、`深度思考中`、`任务已完成`、`503请求异常` 等），杜绝文字过长挤压状态栏信号/电池图标。
* **系统日程级纯黑展开卡片复刻**：
  * 通过 `miui.focus.param` 注入 `protocol: 1` 标准 JSON，系统原生在挖孔下方展开无白边的纯黑毛玻璃卡片（顶部标题 + 右上角大肥鱼 Logo + 极细半透明横线 + 底部操作栏）。

### 3. 彻底根治 SystemUI 卡死软重启（类型安全与资源绑定）
* **痛点根因**：在 `miui.focus.actions` 数据包中，若错误存入原始 `PendingIntent`，系统状态栏进程（SystemUI）在做 `(Notification.Action)` 类型转换时会触发致命的 `ClassCastException` 崩溃；若 JSON 声明了按钮但图片包缺少图标实体，也会引发空指针死循环。
* **彻底修复**：
  * 严格按照规范将按钮打包为 `android.app.Notification.Action` 实体；
  * 在 `miui.focus.pics` 中提供 `miui.focus.pic_action`（白色细线条小闹钟矢量图标 `ic_alarm_white`）；
  * 采用 `Icon.createWithResource` 资源传递机制，零 Binder 显存占用，绝不卡死。

### 4. 彻底静默一级小药丸（执行中绝不强弹大卡片）
* **痛点根因**：此前在更新工具状态时错误调用了 `cancel()` 导致通知被销毁重建，且漏传了 `islandFirstFloat: false`，导致 AI 每执行一步命令就触发一次系统的进场动画大弹窗。
* **彻底修复**：
  * 移除 `cancel()`，采用**纯原地平滑覆写**；
  * 显式设置 `"islandFirstFloat": false` 与 `enableFloat: false`；
  * AI 执行命令时**100% 只在状态栏挖孔两侧静默亮起小药丸**，只有用户主动轻点胶囊时才展开大卡片。

### 5. 单一 ID 原地替换与 120 秒自动消除
* **单 ID 闭环**：全生命周期状态（执行中 / 完成 / 报错 / 终止）统一收拢至单一通知 ID（`Constants.NOTIF_TASK = 2002`），新任务开启时在物理层面直接覆写旧状态，通知栏永远只保留当前最新的唯一卡片；
* **120 秒超时收起**：终态通知（完成 / 报错 / 终止）配置 **`.setTimeoutAfter(120_000L)`**，展示 2 分钟后自动淡出消失，无需手动清理下拉栏。

### 6. 全场景模型异常捕获与精简符号系（方案 B）
* **纯净提取 `LlmFailure`**：只提取 HTTP 状态码与简要报错信息，严格截断前 50 字符，**绝对物理隔离思考流（Reasoning）与命令退出码（Exit Code）**；
* **6 大分支规范**：
  * `❌ 模型请求失败`（如 `Service Unavailable (503)` / `Invalid API Key (401)`）
  * `🔵 任务已中断`
  * `📏 达到单次最大长度`
  * `🛡️ 任务已挂起`
  * `⚡ 连接异常中断`
  * `任务完成`
* **悬浮条同步**：屏幕顶部悬浮条遇到模型 503/400 报错时同步展示 3.5 秒后自然淡出；
* **标题精简**：去除全部通知标题中的 `DSHA · ` 冗余前缀。

### 7. 消除后台 Activity 弹窗抢前台
* 增加 `TaskNotifier.appInForeground` 校验，当用户在使用其他 App 或在桌面时，**严禁在后台 Activity 上弹出 `AlertDialog` 模态对话框**，所有授权操作 100% 留在屏幕顶部的灵动胶囊和通知栏中完成，绝对不打断用户操作。

---

## 二、 核心通知参数映射表

| 场景 | 胶囊左耳 | 胶囊右耳 (4~6字) | 展开黑卡顶部标题 | 底部状态标签与描述 | 右下角药丸按钮 | 浮动级别 | 超时收起 |
| :--- | :--- | :--- | :--- | :--- | :--- | :---: | :---: |
| **任务执行中** | 🐬大肥鱼 | 动态短词 (如`执行命令中`) | 正在执行 | 实时状态 / 步骤描述 | `( ⏰ 停止任务 )` | 静默 (无弹窗) | 常驻 (直到结束) |
| **任务完成** | 🐬大肥鱼 | 任务已完成 | 任务已完成 | 任务完成 / 点击查看 | `( 💬 继续对话 )` | 静默 (无弹窗) | 120 秒 |
| **模型报错** | 🐬大肥鱼 | 请求异常 | ❌ 模型请求失败 | 任务状态 / 503等详情 | `( 💬 重新开始 )` | 静默 (无弹窗) | 120 秒 |
| **任务终止** | 🐬大肥鱼 | 已终止 | ⚠️ 任务已终止 | 任务状态 / 已按指令停止 | `( 💬 重新开始 )` | 静默 (无弹窗) | 120 秒 |
| **危险授权** | 🐬大肥鱼 | 危险授权 | ⚠️ 危险操作授权 | 权限请求 / 具体请求动作 | `( ⏰ 允许授权 )` | 强提醒 (弹大卡) | 用户操作/超时 |
| **命令确认** | 🐬大肥鱼 | 命令确认 | ⚠️ 危险命令确认 | 命令详情 / 具体命令原文 | `( ⏰ 允许 )` | 强提醒 (弹大卡) | 用户操作/超时 |
| **助手提问** | 🐬大肥鱼 | 等待回答 | 💬 助手提问 | 等待回答 / 问题原文 | `( ⏰ 选项一 )` | 强提醒 (弹大卡) | 用户操作/超时 |

---

## 三、 全机型兼容架构

1. **小米澎湃 OS (HyperOS / HyperIsland)**：
   * 优先读取 `miui.focus.param` 与 `miui.focus.pics`，激活原生焦点通知与双耳大肥鱼灵动胶囊；
2. **Google Pixel / 三星 / OPPO / vivo 等 (Android 16+ AOSP)**：
   * 自动识别 `POST_PROMOTED_NOTIFICATIONS` 与 `android.requestPromotedOngoing`，激活各家官方实时动态 / 灵动胶囊；
3. **Android 8.0 ~ 15 老机型**：
   * 平滑降级为标准通知栏卡片，零报错、零崩溃。
