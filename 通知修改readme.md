# DSHA 状态栏灵动胶囊与 HyperOS 焦点通知规范全解析 (通知修改README)

本文档整理并直观绘制出 **小米澎湃 OS (HyperOS / HyperIsland) 焦点通知全协议视觉模板规范**，以及 DSHA 在 **Android 16 官方原生实时活动 (AOSP Live Updates)** 与 **澎湃 OS 灵动岛** 中的双轨技术实现。

---

## 🎨 HyperOS 焦点通知三大官方模板视觉图解

### 模板 ①：日程/详情单按钮模板 (`baseInfo` + `hintInfo`)
* **适用场景**：`任务执行中` (停止任务) / `任务完成` (返回对话)
* **视觉特征**：**带中间细横线，底部右下角单药丸按钮**

```
┌────────────────────────────────────────────────────────┐
│  正在执行                                       [ 🐬 ] │  <-- 顶部：大标题 + 右上角大肥鱼 Logo
├────────────────────────────────────────────────────────┤  <-- 1dp 极细半透明分割线
│  实时状态                                              │  <-- 蓝色状态标签 (hintInfo.content)
│  正在执行命令: git status...        ( ⏰ 停止任务 )   │  <-- 底部：步骤描述 + 右下角单个药丸按钮
└────────────────────────────────────────────────────────┘
```
> ⚠️ **底层死规矩**：分割线下方区域（`hintInfo`）**物理上只支持 1 个单按钮**，绝对无法塞进双按钮！

---

### 模板 ②：来电/决策双动作模板 (`param_v2.actions`，彻底移除 `hintInfo`)
* **适用场景**：`危险操作授权` / `高危命令确认` / `敏感应用操作`
* **视觉特征**：**无分割横线，左侧上下纯文本，右侧垂直居中实心红绿双圆钮 (1:1 复刻来电/闹钟)**

```
┌────────────────────────────────────────────────────────┐
│  ⚠️ 危险命令: rm -f /sdcard/test                       │
│                                           ( ❌ )  ( ✅ ) │  <-- 右侧：实心红绿双圆钮垂直居中 (70%黄金占比)
│  请求调用底层特权执行此命令                            │  <-- 左侧：上方标题 + 下方说明 (100%全宽无遮挡)
└────────────────────────────────────────────────────────┘
```
> 💡 **设计关键**：彻底拿掉横线，右侧实心红圆叉 + 实心绿圆勾居中对齐，左侧文字拉满，完全杜绝空心线圈与横线割裂！

---

### 模板 ③：通栏模态大方块模板 (`textButton`)
* **适用场景**：隔空投送接收文件、应用安装拦截确认
* **视觉特征**：**无横线，底部两个大方块矩形色块占满整行**

```
┌────────────────────────────────────────────────────────┐
│  ⚠️ 危险操作确认                                [ 🐬 ] │
├────────────────────────────────────────────────────────┤
│  [        拒 绝        ]   [        允 许        ]     │  <-- 底部两个巨大方块色块占满整行 (类似弹窗)
└────────────────────────────────────────────────────────┘
```
> ⚠️ **缺点**：过于笨重，属于模态弹窗样式，不推荐用于灵动胶囊。

---

## 📱 一级状态栏挖孔小胶囊视觉图解

### 1. 收起态挖孔小药丸
```
       ┌────── 左耳 ──────┐   (摄像头挖孔)   ┌────── 右耳 ──────┐
       │  [ 🐬 ]  大肥鱼  │       ⚫⚫       │    分析执行中    │
       └──────────────────┘                  └──────────────────┘
```
* **左耳**：透明小蓝鲸图标 + 白色高亮“大肥鱼”副标题；
* **右耳**：动态展示 4~6 字实时状态短词（`执行命令中` / `等待回答` / `任务已完成` / `503请求异常`）。

---

## 📋 全场景通知状态与视觉映射总表

| 场景类别 | 采用模板 | 胶囊左耳 | 胶囊右耳 / AOSP短词 | 展开黑卡核心视觉排版 | 操作按钮规范 | 浮动级别 | 超时收起 |
| :--- | :---: | :--- | :--- | :--- | :--- | :---: | :---: |
| **任务执行中** | **模板 ①** | 🐬大肥鱼 | 动态短词 (如`执行命令中`) | 带横线：上大标题，下步骤详情 | `( ⏰ 停止任务 )` (单药丸) | 默认中级 (不弹大卡) | 常驻 (直到结束) |
| **任务完成** | **模板 ①** | 🐬大肥鱼 | 任务已完成 | 带横线：上大标题，下完成提示 | `( 💬 返回对话 )` (单药丸) | 默认中级 (不弹大卡) | 120 秒 |
| **模型报错** | **模板 ①** | 🐬大肥鱼 | 请求异常 | 带横线：上大标题，下503等详情 | `( 💬 返回对话 )` (单药丸) | 默认中级 (不弹大卡) | 120 秒 |
| **任务终止** | **模板 ①** | 🐬大肥鱼 | 已终止 | 带横线：上大标题，下终止提示 | `( 💬 返回对话 )` (单药丸) | 默认中级 (不弹大卡) | 120 秒 |
| **危险命令确认** | **模板 ②** | 🐬大肥鱼 | 命令确认 | 无横线：左侧纯文本，右侧居中双圆钮 | `( ❌ )` (红) + `( ✅ )` (绿) (70%实心) | 强提醒 (弹大卡) | 用户操作/超时 |
| **危险操作授权** | **模板 ②** | 🐬大肥鱼 | 危险授权 | 无横线：左侧纯文本，右侧居中双圆钮 | `( ❌ )` (红) + `( ✅ )` (绿) (70%实心) | 强提醒 (弹大卡) | 用户操作/超时 |
| **敏感应用确认** | **模板 ②** | 🐬大肥鱼 | 敏感确认 | 无横线：左侧纯文本，右侧居中双圆钮 | `( ❌ )` (红) + `( ✅ )` (绿) (70%实心) | 强提醒 (弹大卡) | 用户操作/超时 |
| **ask_user提问** | **模板 ①** | 🐬大肥鱼 | 等待回答 | 带横线：上大标题，下提问提示 | `( 💬 返回对话 )` (单药丸) | 强提醒 (弹大卡) | 用户操作/超时 |

---

## 🛠️ 双轨底层核心技术规范

1. **HyperOS 三大容器**：
   * `miui.focus.param` (JSON) 负责参数树；
   * `miui.focus.pics` (Bundle) 存放 `Icon` 位图字典；
   * `miui.focus.actions` (Bundle) 存放完整的 `Notification.Action` 实体（严禁传裸 PendingIntent，防闪退卡死）。
2. **Google AOSP 16 标准**：
   * 声明 `POST_PROMOTED_NOTIFICATIONS` 权限；
   * 注入 `android.requestPromotedOngoing = true` 与 `android.shortCriticalText`，自适应 Pixel、Samsung Now Bar、ColorOS 流体云、OriginOS 原子岛等。
